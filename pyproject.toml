[build-system]
requires = ["setuptools>=82.0.1"]
build-backend = "setuptools.build_meta"

[project]
name = "phytospatial"
version = "0.5.1"
authors = [
  { name="Louis-Vincent Grand'Maison", email="lvgra@ulaval.ca" },
]
description = "The open-source standard for enhanced forest inventories"
readme = "README.md"
license = "Apache-2.0"
requires-python = ">=3.10"
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Science/Research",
    "Topic :: Scientific/Engineering :: Image Processing",
    "Topic :: Scientific/Engineering :: GIS",
    "Programming Language :: Python :: 3.10",
    "Operating System :: OS Independent",
]

keywords = ["forestry", "remote sensing", "imagery", "image processing", "geospatial", "GIS"]

# CORE DEPENDENCIES (Lidar, Vector, Raster)
dependencies = [
    "geopandas",
    "pyogrio",
    "polars",
    "pandas",
    "tqdm",
    "rasterio>=1.4.4",
    "numpy>=2.2.6",
    "psutil",
    "python-dotenv",
    "cloth-simulation-filter",
    "scipy",
    "laspy",
    "numba",
    "scikit-image",
    "numexpr",
    "shapely",
]

[project.urls]
"Homepage" = "https://github.com/Louis-Gm/phytospatial"
"Bug Tracker" = "https://github.com/Louis-Gm/phytospatial/issues"

# OPTIONAL DEPENDENCIES
[project.optional-dependencies]
db = [                          # pip install .[db]
    "sqlalchemy>=2.0.49",
    "geoalchemy2",
    "psycopg[binary]",
]

analysis = [                    # pip install .[analysis]
    "pyarrow>=24.0.0",
    "scikit-learn",
]

docs = [                        # pip install .[docs]
    "mkdocs>=1.6.1",
    "mkdocs-material>=9.7.6",
    "mkdocstrings[python]>=1.0.4",
    "mkdocs-include-markdown-plugin>=7.2.2",
    "mkdocs-jupyter>=0.26.3",
    "mkdocs-gen-files",
    "mkdocs-literate-nav",
]

dev = [                         # pip install .[dev]
    "pytest",
    "build",
    "twine",
    "pre-commit",
    "black",
    "memory_profiler",
    "ruff",
]

[tool.setuptools.packages.find]
where = ["src"]
include = ["phytospatial*"]
namespaces = false

[tool.setuptools]
license-files = ["LICENSE", "NOTICE"]

[tool.setuptools.package-data]
"*" = ["LICENSE", "NOTICE"]